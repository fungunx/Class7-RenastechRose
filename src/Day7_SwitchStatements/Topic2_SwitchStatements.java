package Day7_SwitchStatements;

public class Topic2_SwitchStatements {
    public static void main(String[] args) {

        // We use switch statement in a situation simple condition check but a lot of them.
        /*
        variable name
        switch(name)
        case value:
             code block;
             break;  //not mandatory

             case value1;
             code block;
             break;

        default: Code Block
         */
    }
}
